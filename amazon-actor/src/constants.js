const BASE_URL = 'https://www.amazon.com';
const LOG_OFFERS_INTERVAL = 20000; // Interval in ms to log the offers for each ASIN
const SESSION_MAX_USAGE_COUNT = 5;

module.exports = {
    BASE_URL,
    LOG_OFFERS_INTERVAL,
    SESSION_MAX_USAGE_COUNT
}
